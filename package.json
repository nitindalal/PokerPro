{
  "name": "poker-practice-monorepo",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run -w packages/engine build && npm run -w apps/web build",
    "dev": "npm run -w apps/web dev",
    "test": "npm run -w packages/engine test",
    "vercel-build": "cd apps/web && npm ci && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
